# all

This is project functions as wrapper to build all local featjar projects into one single executable.

## Build

```bash
./gradlew build
```
## Run

```bash
./gradlew run --args="--help"
```

or

```bash
java -jar build/libs/feat.jar --help
```
